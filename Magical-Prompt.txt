Create the HTML structure for a single-page application. All content will be inside a main wrapper <div id="app-container">. The application will have several "views" and "modals" that are toggled by JavaScript.Main Container:An outer <div id="app-container"> will hold the entire app UI.Views (Full-screen panels):Auth View (#auth-view):Contains an <h1> with the text "CallingApp".A login form (#login-form) with inputs for email and password, a submit button, and a link to switch to the signup form.A signup form (#signup-form), initially hidden, with the same structure.Include divs for error messages (#login-error, #signup-error).Main App View (#main-view):A <header class="main-header"> containing:A top section with the title "CallingApp" and two icon buttons: "Add Friend" (#add-friend-btn) and "Profile & Settings" (#menu-btn).A navigation section with three tabs: "Chats" (#nav-home), "Updates" (#nav-notifications), and "Calls" (#nav-calls). Each tab should have a <span> for a notification badge.A <main id="main-content"> area with three content panels (.content-panel), one for each tab: #home-content, #notifications-content, and #calls-content.Chat View (#chat-view):A <header class="chat-header"> with a back button, the contact's emoji and name, and three icon buttons: "Voice Call" (#voice-call-btn), "Video Call" (#video-call-btn), and "More options" (#chat-more-btn).A div for messages (#chat-messages).An input container (#chat-input-container) at the bottom with a text input (#chat-input) and a send button (#chat-send-btn).Call Views (.call-view):Video Call View (#video-call-view): Contains a <video> for remote stream (#remote-video), a smaller <video> for local stream (#local-video), and an overlay (.call-overlay) with caller info and an "End Call" button.Voice Call View (#voice-call-view): Contains an <audio> for the remote stream (#remote-audio) and a similar overlay with caller info and an "End Call" button.Modals (Overlays):Create divs for the following modals, all initially hidden:#profile-setup-modal: To set name and emoji after signup.#add-friend-modal: To enter a friend's ID.#profile-view-modal: To show user info, blocked users, and a logout button.#incoming-call-modal: To show an incoming call with "Accept" and "Reject" buttons.Icons:All icons must be inline SVGs inside <button> elements. Use the exact path data from the provided final code.Audio & Scripts:Include an <audio id="ringtone"> element with a loop attribute. Use a minimal, base64-encoded WAV file as its source for a placeholder ringtone.Include the Firebase library scripts for app, auth, and database from the gstatic.com CDN, version 8.10.1.Include a final <script> tag where all the application logic will go.Part 2: CSS StylingInside a <style> tag in the <head>, implement the following design.Fonts and Root Variables:Import the 'Roboto' font from Google Fonts (weights 400, 500, 700).Define a :root block with the following CSS variables for the color scheme:--wa-header-bg: #005c97; (Deep blue)--wa-accent-blue: #34B7F1; (Vibrant blue)--wa-message-out-bg: #e1f6fb; (Light blue for sent messages)--wa-message-in-bg: #ffffff;--wa-app-bg: #f0f2f5; (Light grey app background)--wa-chat-bg: #e5ddd5; (Classic chat background color)--wa-icon-color: #f0f2f5;--wa-text-primary: #111b21;--wa-text-secondary: #667781;--wa-border-color: #e9edef;--wa-active-tab-indicator: var(--wa-accent-blue);--wa-button-color: #008069; (Green for familiarity)--wa-link-color: #00a8e8;--wa-shadow: rgba(17, 27, 33, 0.15);Global & General Styles:Apply a global box-sizing reset (* { box-sizing: border-box; }).Style html and body to be 100% height/width, overflow: hidden, and center the app container using flexbox. Set the outer page background to #d1d7db.Add styles for a custom WebKit scrollbar.Style the generic .icon-btn class.Layout & View Styling:Style #app-container to have a max-width of 450px, a max-height of 950px, a box shadow, and rounded corners.Style the .view class using flexbox for a column layout.Component-Specific Styling:Auth View: Style the title, forms, inputs (with :focus states), buttons, and links.Main View:Style the .main-header, .header-top, and .header-nav.Style the .nav-tab, making sure to include styles for the .active class with a bottom border and different color.Style the .badge for notifications.List Items: Create a flexible .list-item style for contacts, notifications, and call logs, including sections for an emoji, details (name, subtext), and actions.Chat View:Style the .chat-header and its components.For the #chat-messages background, use the #wa-chat-bg color and apply a subtle repeating background pattern using this exact SVG data URI: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%23bdbdbd' fill-opacity='0.15' fill-rule='evenodd'%3E%3Cpath d='M0 40L40 0H20L0 20M40 40V20L20 40'/%3E%3C/g%3E%3C/svg%3E")Style .message-bubble, with variants for .message-sent (right-aligned, light blue, different border-radius) and .message-received (left-aligned, white).Style the #chat-input-container and the rounded #chat-input and #chat-send-btn.Call Views:Style the .call-view to be an absolute-positioned overlay.Style the remote and local video elements, including the draggable look for #local-video.Style the .call-overlay with a gradient, and the .caller-info and .call-controls within it. Use specific colors for the end (#e91e63) and accept (#4CAF50) call buttons.Modals: Style the .modal overlay and the .modal-content box.Part 3: JavaScript LogicInside the final <script> tag, implement all application logic using Firebase for the backend and WebRTC for calls.Setup:Firebase Config: Initialize Firebase using a placeholder firebaseConfig object with the exact keys and dummy values as seen in the final code.Global State: Declare global variables for currentUser, currentChatPartner, peerConnection, localStream, etc.Utility Functions: Create helper functions showView(), showModal(), and showPanel() to toggle UI visibility.Core Functionality (in order):Authentication:Use auth.onAuthStateChanged to manage the user's session.If a user logs in, listen to their data in the users node. If their profile doesn't exist, show the profile setup modal. If it does, initialize the app and show the main view.If the user logs out, show the auth view.Add event listeners for the login, signup, and logout forms/buttons.Profile Management:Implement the logic to save a new user's profile (name, emoji, generated ID) to the Firebase Realtime Database.Implement the logic for the profile modal: display user info, allow editing, and show a list of blocked users.Friend & Contact System:Implement "Add Friend": When a user enters an ID, find the corresponding user in the database and create a "request" in a requests/{recipient_uid} node.Listen for friend requests in requests/{currentUser.uid} and display them in the "Updates" tab. Provide functions to accept or reject requests. Accepting a request should add each user to the other's contacts list.Listen to the contacts node to populate the "Chats" list.Messaging:When a contact is clicked, open the chat view.Listen for messages in the messages/{chat_id} node (where chat_id is a sorted combination of the two user UIDs). Display messages, distinguishing between sent and received.Implement sending messages: push the message object to Firebase and update the recipient's unread message count in a separate unreadCounts node.Listen for changes in unreadCounts to display notification badges.WebRTC Calling Logic:Start Call: When a call button is clicked:Get user's audio/video stream (getUserMedia).Create an RTCPeerConnection.Create an SDP offer.Set the offer as the local description.Write the call data (including the offer) to the database under calls/{callee_uid}.Listen for ICE candidates and write them to the DB under iceCandidates/{call_id}/caller.Receive Call:Listen for new entries at calls/{currentUser.uid}. When one appears, show the incoming call modal.On Accept: Get user media, create RTCPeerConnection, set the remote description from the received offer, create an answer, set it as the local description, and update the call entry in the DB with the answer. Listen for ICE candidates from the caller.On Reject: Remove the call entry from the database.Connection: Both peers listen for the other's ICE candidates and add them to their RTCPeerConnection. Listen for the ontrack event to display the remote stream.End Call: Implement an endCall function that closes the connection, stops media tracks, and removes all related call data from Firebase (calls and iceCandidates nodes). Log the call to a callLogs node.User Blocking:Implement functionality to add a user's UID to a blocked list under the current user's profile.Check this blocked list before displaying contacts, processing friend requests, or allowing incoming calls from a blocked user.Provide a way to "unblock" users from the profile modal.Ensure all event listeners are correctly attached to the HTML elements defined in Part 1. The code should be well-structured with comments separating the major functional blocks (Auth, Profile, Messaging, WebRTC, etc.). bhai isme emoji ke bajaye username ka system rakho aur user ko username se friend request bhej ke kaam ho aesa karo.
